spring:
  application:
    name: Hospital-Springboot
  datasource:
    url: jdbc:mariadb://localhost:3306/hospita_springboot?createDatabaseIfNotExist=true
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
  jpa:
    show-sql: true
